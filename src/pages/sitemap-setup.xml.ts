import type { APIRoute } from 'astro';import { getCollection } from 'astro:content';
export const GET:APIRoute=async({site})=>{const origin=site??new URL('https://nocharge.net');const entries=(await getCollection('setup')).filter(a=>!a.data.draft).sort((a,b)=>b.data.publishedDate.localeCompare(a.data.publishedDate));const rows=['/setup/',...entries.map(a=>`/setup/${a.id}/`)].map(path=>`  <url><loc>${new URL(path,origin)}</loc></url>`).join('\n');return new Response(`<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n${rows}\n</urlset>\n`,{headers:{'Content-Type':'application/xml; charset=utf-8'}})};
