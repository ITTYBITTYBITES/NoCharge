import type { APIRoute } from 'astro';import { getCollection } from 'astro:content';
const esc=(s:string)=>s.replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('\"','&quot;');
export const GET:APIRoute=async({site})=>{const origin=site??new URL('https://nocharge.net');const entries=(await getCollection('setup')).filter(a=>!a.data.draft).sort((a,b)=>b.data.publishedDate.localeCompare(a.data.publishedDate));const items=entries.map(a=>`  <item><title>${esc(a.data.title)}</title><link>${new URL(`/setup/${a.id}/`,origin)}</link><guid>${new URL(`/setup/${a.id}/`,origin)}</guid><description>${esc(a.data.description)}</description><pubDate>${new Date(`${a.data.publishedDate}T12:00:00Z`).toUTCString()}</pubDate><category>${esc(a.data.topic)}</category><nocharge:reviewed>${a.data.reviewedDate}</nocharge:reviewed></item>`).join('\n');return new Response(`<?xml version="1.0" encoding="UTF-8"?>\n<rss version="2.0" xmlns:nocharge="https://nocharge.net/ns/editorial"><channel><title>Quiet Setup</title><link>${new URL('/setup/',origin)}</link><description>Practical NoCharge setup guides without assuming a purchase is necessary.</description><language>en-us</language>\n${items}\n</channel></rss>\n`,{headers:{'Content-Type':'application/rss+xml; charset=utf-8','X-Robots-Tag':'noindex, follow'}})};
