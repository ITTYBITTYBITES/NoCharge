import {describe,expect,it} from 'vitest';import {createPuzzle,solvedGrid,solve,isValidMove,togglePencilMarks} from './engine';
describe('mini sudoku engine',()=>{it('makes valid solved grids',()=>{for(let s=1;s<20;s++){const g=solvedGrid(s);expect(g.flat()).toHaveLength(36);expect(solve(g,2)).toHaveLength(1);}});it('makes unique documented puzzles',()=>{for(const d of ['easy','medium','hard'] as const){const p=createPuzzle(d,22);expect(p.puzzle.flat().filter(Boolean)).toHaveLength({easy:24,medium:20,hard:16}[d]);expect(solve(p.puzzle,2)).toHaveLength(1);}});it('validates moves and pencil marks',()=>{const g=solvedGrid();expect(isValidMove(g,0,0,g[0][0])).toBe(true);expect(isValidMove(g,0,0,g[0][1])).toBe(false);expect(togglePencilMarks(new Set([1]),1).size).toBe(0);});});
