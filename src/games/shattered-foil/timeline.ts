export interface MoveEvent<T>{id:number;description:string;scoreDelta:number;elapsedMs:number;state:T}export interface Timeline<T>{events:MoveEvent<T>[];liveIndex:number;viewIndex:number}export const createTimeline=<T>(initial:T):Timeline<T>=>({events:[{id:0,description:'Deal started',scoreDelta:0,elapsedMs:0,state:structuredClone(initial)}],liveIndex:0,viewIndex:0});export function appendMove<T>(timeline:Timeline<T>,event:Omit<MoveEvent<T>,'id'>):Timeline<T>{const base=timeline.events.slice(0,timeline.liveIndex+1),next={...event,id:base.length};return{events:[...base,next],liveIndex:base.length,viewIndex:base.length};}export function viewStep<T>(timeline:Timeline<T>,index:number):Timeline<T>{return{...timeline,viewIndex:Math.max(0,Math.min(index,timeline.liveIndex))};}export const viewedState=<T>(timeline:Timeline<T>)=>structuredClone(timeline.events[timeline.viewIndex].state);
