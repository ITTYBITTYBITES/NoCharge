export class GlassFoley{private ctx:AudioContext|null=null;volume=.7;enabled=true;private context(){if(!this.ctx)this.ctx=new AudioContext();return this.ctx;}setVolume(v:number){this.volume=Math.max(0,Math.min(1,v));}private tone(freq:number,duration:number,type:OscillatorType='sine'){if(!this.enabled)return;const c=this.context(),o=c.createOscillator(),g=c.createGain(),now=c.currentTime;o.type=type;o.frequency.value=freq;o.connect(g);g.connect(c.destination);g.gain.setValueAtTime(Math.max(.001,this.volume*.1),now);g.gain.exponentialRampToValueAtTime(.0001,now+duration);o.start(now);o.stop(now+duration);}glassTap(){this.tone(920,.09,'sine');}copperSnap(){this.tone(310,.13,'triangle');}foundationChime(){this.tone(660,.3);setTimeout(()=>this.tone(990,.25),70);}win(){[523,659,784,1047].forEach((f,i)=>setTimeout(()=>this.tone(f,.45),i*90));}destroy(){void this.ctx?.close();this.ctx=null;}}
