#!/usr/bin/env node
import {readFileSync} from 'node:fs';const feed=readFileSync('dist/setup/feed.xml','utf8');if(!feed.startsWith('<?xml')||!feed.includes('<rss'))throw new Error('Invalid setup RSS root.');const items=[...feed.matchAll(/<item>/g)].length;if(items!==30)throw new Error(`Expected 30 feed items, found ${items}.`);for(const required of ['<nocharge:reviewed>','<category>','https://nocharge.net/setup/'])if(!feed.includes(required))throw new Error(`Feed missing ${required}`);if(/amazon\.com|<script|tracking|pixel/i.test(feed))throw new Error('Feed contains merchant or tracking content.');console.log('Quiet Setup RSS validation passed with 30 items.');
